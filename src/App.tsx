import React from 'react';

import { CalculatorPage } from 'pages/CalculatorPage';

export default function App() {
    return (
        <React.StrictMode>
            <CalculatorPage />
        </React.StrictMode>
    );
}
